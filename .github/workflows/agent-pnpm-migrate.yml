name: Migrate project workflow to pnpm

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]

permissions:
  contents: write

jobs:
  migrate:
    if: github.head_ref == 'agent/migrate-pnpm-workflow'
    uses: sena-nana/LiliaUI/.github/workflows/reusable-pnpm-migration-v2.yml@da338d73cb3e0c6c99371198852892985a18e370
    with:
      lilia_ui_ref: 0cbd959c46270d3191a08d1fab3def098d81188a
